# Rustamoff jeans Android APK

Branch: `apk-build`

- App name: Rustamoff jeans
- Supabase session is persisted by the web app and WebView DOM storage, so the password is normally required only on first successful login.
- Splash screen is included.
- File/image chooser support is enabled in the Android WebView.

GitHub Actions workflow: `.github/workflows/build-apk.yml`.
